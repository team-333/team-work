<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 	
		"-//mybatis.org//DTD Mapper 3.0//EN" 
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="com.itbank.dao.StudyDAO">
	<select id="selectAllStudies" resultType="StudyVO">
		select * from team order by teamid asc
	</select>
	
	<insert id="insertStudy" parameterType="StudyVO">
		insert into team (TEAMNAME, TEAMINFO, TEAMPUBLIC, TEAMPICTURE) values (#{teamName}, #{teamInfo}, #{teamPublic}, #{teamPicture})
	</insert>
	
</mapper>
		
